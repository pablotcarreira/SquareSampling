; the next section is mandatory
[general]
name=Square Sampling
qgisMinimumVersion=2.0
description=Creates fixed size squares by clicking on a map.
category=Vector
version=version 0.1
author=Pablo Carreira
email=pablotcarreira@gmail.com
; end of mandatory metadata

; start of optional metadata
changelog=
    0.1: Code cleaning.

; tags are in comma separated value format, spaces are allowed
tags=digitizing,vector,sampling

; these metadata can be empty
; in a future version of the web application it will
; be probably possible to create a project on redmine
; if they are not filled
homepage=http://hub.qgis.org/projects/SquareSampling
tracker=http://hub.qgis.org/projects/SquareSampling
repository=https://github.com/pablotcarreira/SquareSampling
icon=icons/icon.png

; experimental flag
experimental=False

; deprecated flag (applies to the whole plugin and not only to the uploaded version)
deprecated=False